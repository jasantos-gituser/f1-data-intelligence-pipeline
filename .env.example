# Supabase PostgreSQL — append ?options=-c%20search_path%3Df1_stats_schema to target the correct schema
DATABASE_URL=postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres?options=-c%20search_path%3Df1_stats_schema

# Anthropic — required for ?mode=ai predictions
ANTHROPIC_API_KEY=

# API auth — generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=y7Y5r7aJim_ovnMHc672T57ovdp3QxjT137AEZI3Dww

# Optional overrides
INGEST_INTERVAL_HOURS=6
OPENF1_TIMEOUT_SECONDS=30
PREDICTION_CACHE_ENABLED=true
ENVIRONMENT=development
